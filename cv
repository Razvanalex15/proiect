<!DOCTYPE html>
<html lang="ro">
<head>
  <meta charset="UTF-8">
  <title>AutoBazar Chat</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>

<div class="app">

  <!-- SIDEBAR -->
  <aside class="left">
    <div class="side-header">
      <h3>AutoBazar</h3>
      <span>inbox • mesaje</span>
    </div>

    <input class="search" placeholder="Caută conversație">

    <div class="chat-item active">
      <b>BMW 320d • Vânzător</b>
      <span>„Mai e disponibilă?”</span>
    </div>

    <div class="chat-item">
      <b>Golf 7 • Cumpărător</b>
      <span>Ok, trimit poze 👍</span>
    </div>
  </aside>

  <!-- CHAT -->
  <main class="chat">

    <header class="top">
      <div>
        <b>Asistent AutoBazar</b>
        <small class="status">
        <span class="dot"></span> activ acum
</small>

      </div>
    </header>

    <section class="msgs">
      <div class="msg left">
        Salut! 👋 Ce vrei să faci?
      </div>

      <div class="msg right">
        Vreau să vând o mașină 🚗
      </div>

      <div class="msg left">
        Perfect. Încarcă poze și adaugă prețul
      </div>
    </section>

    <!-- INPUT BAR -->
    <footer class="bar">
      <button class="ico">➕</button>
      <button class="ico">😊</button>

      <input class="in" placeholder="Scrie un mesaj">

      <button class="ico">🎤</button>
      <button class="send">➤</button>
    </footer>

  </main>

</div>

</body>
</html>






















/* =========================
   RESET
   ========================= */
* {
  box-sizing: border-box;
  margin: 0;
  font-family: Arial, sans-serif;
}

/* =========================
   PAGE (full screen)
   ========================= */
body {
  min-height: 100vh;
  background: #ffffff;
  color: #111827;
}

/* =========================
   APP LAYOUT
   ========================= */
.app {
  width: 100vw;
  height: 100vh;
  display: grid;
  grid-template-columns: 280px 1fr;
  background: #ffffff;
}

/* =========================
   SIDEBAR (LEFT)
   ========================= */
.left {
  background: #0f1720;
  padding: 12px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.left input {
  padding: 8px 10px;
  border-radius: 6px;
  border: none;
  outline: none;
  background: #111c26;
  color: #e5e7eb;
}

/* conversation items */
.chat-item {
  padding: 10px;
  border-radius: 8px;
  cursor: pointer;
}

.chat-item span {
  display: block;
  font-size: 12px;
  color: #9ca3af;
}

.chat-item.active {
  background: rgba(37, 99, 235, 0.25);
  border-left: 3px solid #2563eb;
}

/* =========================
   CHAT (RIGHT)
   ========================= */
.chat {
  display: flex;
  flex-direction: column;
  background: #0b1117;
}

/* top bar */
.top {
  padding: 12px;
  background: #111c26;
  border-bottom: 1px solid #1f2a37;
}

/* status (dot + text) */
.status {
  font-size: 12px;
  color: #9ca3af;
  display: flex;
  align-items: center;
  gap: 6px;
}

.dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #22c55e;
}

/* messages area */
.msgs {
  flex: 1;
  padding: 14px;
  overflow-y: auto;
}

/* message bubble */
.msg {
  max-width: 70%;
  padding: 10px 12px;
  margin-bottom: 8px;
  border-radius: 10px;

  background: #111c26;
  color: #e5e7eb; /* IMPORTANT: altfel textul devine invizibil */
}

/* user message */
.msg.right {
  background: #1e40af;
  color: #ffffff;
  margin-left: auto;
}

/* =========================
   INPUT BAR
   ========================= */
.bar {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px;
  border-top: 1px solid #1f2a37;
  background: #0f1720;
}

.bar input {
  flex: 1;
  padding: 8px 12px;
  border-radius: 20px;
  border: none;
  outline: none;
  background: #111c26;
  color: #e5e7eb;
}

/* buttons (general) */
.bar button {
  padding: 0 14px;
  border: none;
  border-radius: 20px;
  background: #2563eb;
  color: #ffffff;
  cursor: pointer;
}

