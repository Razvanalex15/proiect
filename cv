app.ts
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';
 
const port = 5000;
 
async function bootstrap() {
  const app = await NestFactory.create(AppModule, { cors: true });
 
 
  app.enableCors({
    origin: `http://localhost:${port}`,
    credentials: true,
  });
   await app.listen(port);
  bootstrap();
}
app.module.ts
import {Module} from "@nestjs/common";
import { UserModule } from "./user-module/user.module";
 
@Module({
    imports:[UserModule],
    providers:[],
    exports:[]
})
export class AppModule {}
ts.config
{
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "esModuleInterop": true,
    "target": "ES2020",
    "skipDefaultLibCheck":true,
    "module": "commonjs",
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "moduleResolution": "node",
  },
  "exclude": ["node_modules"],
  "include": ["src/**/*"]
}
am problema asta de nu-mi poate vedea acest modul desi el exista  si nu stiu ce nu-i place
 
la npm run build nu apare nicio eroare numai la npm run dev imi zice ca nu gaseste modulu stiee cineva de ce este aceasta eroare?
 
